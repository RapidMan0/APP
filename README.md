# React Native Movie App 🎬

Приложение для просмотра фильмов на React Native с использованием TMDB (The Movie Database) API.

## Функциональность

✅ Просмотр популярных фильмов  
✅ Поиск фильмов по названию  
✅ Детальная информация о фильме (описание, рейтинг, бюджет и т.д.)  
✅ Информация об актёрах  
✅ Разделение на две вкладки (Главная и Поиск)  

## Установка и Запуск

### 1. Получить API ключ от TMDB

1. Перейди на https://www.themoviedb.org/settings/api
2. Зарегистрируйся или войди в свой аккаунт
3. Скопируй свой API ключ (API Key)

### 2. Добавить API ключ в приложение

Открой файл `src/constants/config.js` и замени:
```javascript
export const TMDB_API_KEY = 'YOUR_TMDB_API_KEY_HERE';
```

На свой ключ:
```javascript
export const TMDB_API_KEY = 'твой_api_ключ_здесь';
```

### 3. Запустить приложение

```bash
# Для запуска на Android
npm run android

# Для запуска на iOS
npm run ios

# Для запуска на веб-версии
npm run web

# Базовый запуск (выбирешь платформу после)
npm start
```

## Структура проекта

```
APP/
├── src/
│   ├── components/      # UI компоненты
│   │   └── MovieCard.js
│   ├── screens/         # Экраны приложения
│   │   ├── HomeScreen.js
│   │   ├── SearchScreen.js
│   │   └── MovieDetailsScreen.js
│   ├── services/        # API сервисы
│   │   └── tmdbService.js
│   └── constants/       # Константы конфига
│       └── config.js
├── App.js              # Главный файл с навигацией
├── app.json            # Конфиг Expo
└── package.json        # Зависимости
```

## Используемые библиотеки

- **React Native** - основной фреймворк
- **Expo** - платформа для разработки
- **React Navigation** - навигация между экранами
- **Axios** - HTTP клиент для API запросов
- **Expo Vector Icons** - иконки материального дизайна

## API Endpoints (используются в приложении)

- `/movie/popular` - Популярные фильмы
- `/movie/now_playing` - Фильмы в кино
- `/movie/top_rated` - Лучшие фильмы по рейтингу
- `/movie/upcoming` - Предстоящие фильмы
- `/search/movie` - Поиск фильмов
- `/movie/{id}` - Детали фильма с актёрами и рекомендациями

## Готовые функции для использования

Все функции находятся в `src/services/tmdbService.js`:

```javascript
getPopularMovies(page)      // Получить популярные фильмы
getNowPlayingMovies(page)   // Фильмы в кино
getTopRatedMovies(page)     // Лучшие рейтинговые
getUpcomingMovies(page)     // Предстоящие
searchMovies(query, page)   // Поиск
getMovieDetails(movieId)    // Детали фильма
getMovieVideos(movieId)     // Видео/трейлеры
```

## Как расширить приложение

### Добавить новую категорию фильмов

1. Создай новый экран в `src/screens/`
2. Используй нужный метод из `src/services/tmdbService.js`
3. Добавь новую вкладку в `App.js`

### Добавить локальное сохранение избранного

```bash
npm install @react-native-async-storage/async-storage
```

### Добавить видеоплеер для трейлеров

```bash
npm install react-native-video expo-av
```

## Решение проблем

### API ключ не работает
- Проверь, что скопировал точный ключ из TMDB
- Убедись, что добавил его правильно в `config.js`
- Перезагрузи приложение

### Фильмы не загружаются
- Проверь интернет соединение
- Посмотри console.log в ошибках
- Убедись, что API ключ валидный

### Изображения не показываются
- Это может быть из-за сети - дай время на загрузку
- Проверь, что `TMDB_IMAGE_BASE_URL` правильный в config.js

## Лицензия

MIT - используй свободно для своих проектов!

---

**Готово к разработке!** 🚀 Удачи в создании!
